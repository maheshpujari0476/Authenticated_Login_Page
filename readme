

// If u want see only the output pages then use browser after this install the postman tool u find each and every process 
// that how the process is going on and how is request and reponse cycle happens its clear while using the postman










// => this is the simple login based basic project based on the user login credentials mostly i used backend so just ignore frontend

// => as of iam familear with backend so i just wanted know that how the new user logsin and logout , how the password is stored into hash form
// and how the session ,cookies and tokens are generated and how they are verified while login into a aprticular website i made this for my sake i wanted learn this all
// things how the request flows from frontend to backend and how the server responds to the request 

//to start the server give npm start in the terminal


//before running the code u need to install some packeges those sre below just install all these packages before rinnnuing the code
// => npm install epress ==>"express": "^5.1.0",

//npm install session "express-session": "^1.18.1",

// npm install jsonwebtoken ==> "jsonwebtoken": "^9.0.2",

// npm install mongoose ==>"mongoose": "^8.13.2",

// npm install multer ==>"multer": "^1.4.5-lts.2",

//  npm install nodemon ==>"nodemon": "^3.1.9"

// npm install argon ==>  "argon2": "^0.41.1",

// this is optinal if u using bcrypt then use it = npm install bcrypt ==> "bcrypt": "^5.1.1",

//  npm install bodyParser ==> "body-parser": "^2.2.0",

// this shows the colors of the request arrived from the client ==> npm install colors "colors": "^1.4.0",

// "connect-mongo": "^5.1.0",

// npm install cookie "cookie-parser": "^1.4.7",

// npm install cors ==> "cors": "^2.8.5",

// npm install crypto ==> "crypto": "^1.0.1",

// npm install ejs ==>"ejs": "^3.1.10",

// I have used the mongodb to connect and save the date in the database using mongoose shell 
// and argon is for hasing the password this method is one of the most strongest hashing to to hash and store the password in the database

//http://localhost:3000 => this is the api were the server will bee running when the server is started

//=> In this server is the mainserver file which handles the routes based on the requests

// => At he the moment i have created only one Schema based on the user login credentials 


//==> Coming to registration part just go to http:localhost:3000/user/register 

// ==> You wil find the blank page beacuse i used only html using ejs only to render the pages  

//==> has off u see the credentials just fill the data and submit u will find the new page has welcome from user

//=> If u see the welcome page then you registration part is completed and your data is saved in database using mongoDB

// => once completion of registration go to login
//-------------------------------------------------------------------------------------------------


//==>> For login   http:localhost:3000/user/login

// => their u find email and password enter the correct email and password if these are wrong u cant be loged im
// when u login into the page u find anothe mainpage welcom page that u haved loged in successfully 




//==> Basucally i have created a secrest.js file which u cant access without your token ,session,cookies
// SO main thing is when u login the server automatically creates a session and tokens and cookies which are most important in this generation
// when u want access the mainpage example if you want to by something in amazom you have registered but not loged in it will show u to 
// first login into the page and when u login however the tokenes ,session or cookies are created these are directly verified by the server and 
//if u are authenticated then only it will allow u to buy somthing in the amazon


//=> seeing the when u login inti my this http://localhost:3000/user/mainpage  it is only accessed when the person is fully authenticated
// otherwise it will be deneid access so follow the above steps correctly


//=> if u want to upload the files this is also there i used multer to accept the file which is most commonly used mehtod 

// => this multer is for to accept the file from client and send it to its destination as off it takes only two arguments one filename and another destination where to store the upload the file

//=> if u want want know first basucs of all these go the file hello.js see from starting u find the code some simple to advanced code but my preference is to use the postman